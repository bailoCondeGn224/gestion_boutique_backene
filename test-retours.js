const axios = require('axios');

// Simuler une requête avec un vrai token (récupérer depuis localStorage ou demander à l'utilisateur)
console.log('Test endpoint /retours/client');
console.log('Structure attendue: { data: [], meta: {...} }');
console.log('\nNote: Besoin d\'un token JWT valide pour tester réellement');
